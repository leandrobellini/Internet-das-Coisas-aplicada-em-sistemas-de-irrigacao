<!-- =================== Header =================== -->
<%  include ./partials/header %>
<!-- =================== /Header =================== -->
<canvas id="myChart"></canvas>

<!-- =================== Footer =================== -->
<% include ./partials/footer %>
<!-- =================== /Footer =================== -->


<script type="text/javascript">
    var ctx = document.getElementById("myChart").getContext("2d");

    var timeFormat = 'MM/DD/YYYY HH:mm';

    var vetorPressao = [];
    <% pressao.forEach(function(p){
    %>
      vetorPressao.push('<%= p %>');
    <%
    })
    %>

    var vetorData = [];
    <% datas.forEach(function(data){
    %>
      vetorData.push('<%= data %>');
    <%
    })
    %>

    var config = {
        type: 'line',
        data: {
          labels: vetorData,

          datasets: [{
            type: 'line',
            label: 'Pressão atmosférica- Últimas 24 horas',
            backgroundColor: "#40bf40",
            borderColor: "#40bf40",
            fill: false,
            data: vetorPressao,
            borderWidth: 2,
            pointRadius: 0
          }]
        },
        options: {
            title: {
                text:"Umidade do Solo"
            },
            scales: {
              xAxes: [{
                type: "time",
                display: true,
                time: {
                  format: timeFormat,
                  displayFormats: {
                    'hour': 'MMM DD, HH:mm'
                  }
                }
              }]
            }
          }
      };
  
  var myChart = new Chart(ctx, config);


</script>